package com.m1technology.stratostore.service;

public class RandomServiceRandomOrgImpl implements RandomService {
    @Override
    public byte[] getKey(Integer length) {
        return new byte[0];
    }
}
